import React from "react";
import EMDropdownItem from "../dropdown_components/EMDropdownItem";

//component - CoreUI / EMDropdownHeader

const EMDropdownHeader = (props) => {
  return <EMDropdownItem {...props} header />;
};

export default EMDropdownHeader;
