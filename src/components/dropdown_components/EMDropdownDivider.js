import React from "react";
import EMDropdownItem from "../dropdown_components/EMDropdownItem";

//component - CoreUI / EMDropdownDivider

const EMDropdownDivider = (props) => {
  return <EMDropdownItem {...props} divider />;
};

export default EMDropdownDivider;
